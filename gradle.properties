kotlin.code.style=official
org.gradle.jvmargs=-Xmx1G
org.gradle.java.home=C:/Program Files/AdoptOpenJDK/jdk-16.0.1.9-hotspot

# Fabric Properties
	# Check these on https://modmuss50.me/fabric.html
	minecraft_version=1.17-pre1
	yarn_mappings=1.17-pre1+build.1
	loader_version=0.11.3

	#Fabric api
	fabric_version=0.34.8+1.17
	loom_version=0.8-SNAPSHOT

	# Mod Properties
	mod_version = 0.2.3
	maven_group = net.barribob
	archives_base_name = mobs-attempt-parkour

	# Kotlin
	kotlin_version=1.4.21
	fabric_kotlin_version=1.4.21+build.1

	# Maelstrom Library
	maelstrom_version = 1.2.1

	# A hacky parameter variable so that I can build without commenting stuff out
	# This should always be false, and then when you build, include -PdoBuild=true to set it to true.
	doBuild = false