kotlin.code.style=official
org.gradle.jvmargs=-Xmx1G
org.gradle.java.home=C:/Program Files/AdoptOpenJDK/jdk-16.0.1.9-hotspot

# Fabric Properties
	# Check these on https://modmuss50.me/fabric.html
	minecraft_version=1.18
	yarn_mappings=1.18+build.1
	loader_version=0.12.8

	#Fabric api
	fabric_version=0.43.1+1.18
	loom_version=0.10-SNAPSHOT

	# Mod Properties
	mod_version = 0.2.4
	maven_group = net.barribob
	archives_base_name = mobs-attempt-parkour

	# Kotlin
	kotlin_version=1.6.0
	fabric_kotlin_version=1.7.0+kotlin.1.6.0

	# Maelstrom Library
	maelstrom_version = 1.2.3
	minecraft_maelstrom_version = 1.18-rc3

	# A hacky parameter variable so that I can build without commenting stuff out
	# This should always be false, and then when you build, include -PdoBuild=true to set it to true.
	#  gradlew clean build -PdoBuild=true -Dorg.gradle.java.home="C:\Program Files\Eclipse Adoptium\jdk-17.0.1.12-hotspot"
	doBuild = false 